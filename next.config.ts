/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Mera-Dukan-Mera-Godam', // Aapka repo name '/' ke saath
  images: { unoptimized: true },
};


export default nextConfig;
